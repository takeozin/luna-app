const fs = require('fs'); const content = fs.readFileSync('app/data/activitiesData.ts', 'utf8'); const regex = /"(\d{3})":\s*\{[\s\S]*?steps:\s*\[([\s\S]*?)\]\n\s*\},/g; let match; while ((match = regex.exec(content)) !== null) { const stepsStr = match[2]; const stepCount = (stepsStr.match(/text:/g) || []).length; if (stepCount < 2) console.log(match[1] + ' has ' + stepCount + ' steps'); }
